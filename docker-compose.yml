services:
  trivy-ui:
    build: .
    image: trivy-ui:latest
    container_name: trivy-ui
    ports:
      - "8569:8569"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./trivy-cache:/var/lib/trivy
    restart: unless-stopped

